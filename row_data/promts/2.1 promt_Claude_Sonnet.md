Convierte exclusivamente tu respuesta inmediatamente anterior en un archivo CSV estructurado.

## Objetivo

Extraer todas las triadas incluidas en la sección **“Triadas RDF candidatas”** de tu respuesta anterior y convertir cada una en una fila del CSV.

la información que no se encuentre en esta sesión la completas con las otras sesiones de la misma respuesta

No vuelvas a analizar el corpus, no consultes conocimiento externo, no agregues triadas nuevas y no cambies la semántica de las triadas que ya generaste.

## Dato entrada: 
* LLM_3

## Reglas obligatorias

1. Usa únicamente la información de tu respuesta anterior.
2. Cada triada RDF candidata debe ocupar una fila.
3. Conserva exactamente el sujeto, predicado y objeto que propusiste.
4. Conserva la evidencia, fuente, ubicación, tipo de evidencia, confianza y necesidad de validación experta cuando estén disponibles en tu respuesta anterior.
5. Si algún dato no aparece explícitamente en tu respuesta anterior, escribe `NOT_SPECIFIED_IN_PREVIOUS_OUTPUT`.
6. No inventes identificadores de fuente, fragmentos, páginas, niveles de confianza ni justificaciones.
7. No normalices sinónimos ni modifiques términos.
8. No conviertas las triadas en axiomas OWL.
9. No incluyas explicaciones, introducciones ni conclusiones.
10. Devuelve únicamente un bloque de código CSV.
11. Usa punto y coma `;` como separador de columnas.
12. Todos los valores deben ir entre comillas dobles.

## Metadatos de esta ejecución

* `llm_id`: `[REEMPLAZAR, por ejemplo: LLM_1]`
* `question_id`: `[REEMPLAZAR, por ejemplo: ALC_02]`

## Encabezado obligatorio

```csv
"triad_id";"llm_id";"question_id";"subject";"predicate";"object";"rdf_candidate";"source_id";"source_location";"evidence_text";"evidence_type";"confidence";"expert_validation_required";"candidate_status";"notes"
```

## Formato para cada fila

* `triad_id`: usa el patrón `[llm_id]_[question_id]_[consecutivo]`, por ejemplo: `LLM_1_ALC_02_001`.
* `llm_id`: usa el valor indicado en los metadatos.
* `question_id`: usa el valor indicado en los metadatos.
* `subject`, `predicate`, `object`: copia los elementos de la triada anterior.
* `rdf_candidate`: escribe la triada en Turtle, por ejemplo: `:BioreactorSystem :hasComponent :Sensor .`
* `candidate_status`: usa siempre `CANDIDATE`.
* `notes`: registra observaciones ya expresadas en tu respuesta anterior; si no existen, usa `NOT_SPECIFIED_IN_PREVIOUS_OUTPUT`.

Genera ahora el CSV a partir de tu respuesta inmediatamente anterior.